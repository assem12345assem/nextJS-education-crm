import {baseUrl} from "@/app/config/baseUrl";
import {Main} from "@/app/models/main";
import axios from "axios";
import React from "react";

// @ts-ignore
const SampleMainPage = ({main}: Main) => {

    return (
        <div>
            <p>Main Page</p>
        </div>
    );
};

export default SampleMainPage;

